# shellcheck shell=bash disable=SC2034 # sourced by load_profile
# Hardware-neutral starting point: copy this for a new machine and list the
# hardware aspects (modules/base/install-<aspect>.sh) it needs.
MACHINE_ASPECTS=()
