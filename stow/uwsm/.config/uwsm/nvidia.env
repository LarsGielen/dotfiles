# NVIDIA-specific session environment. Sourced by ./env only when the nvidia
# kernel module is loaded, so the same stow package works on any GPU.
export LIBVA_DRIVER_NAME=nvidia
export __GLX_VENDOR_LIBRARY_NAME=nvidia
export GBM_BACKEND=nvidia-drm
